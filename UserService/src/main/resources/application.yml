spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/userservice
    username: root
    password: root
  application:
    name: UserService  
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
server:
  port: 2021   
  
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    #niha eureka
    service-url.defaultZone: http://10.81.4.222:8761/eureka/
         
